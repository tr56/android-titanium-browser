name: Build

on:
  workflow_dispatch:
    inputs:
      runner:
        description: 'Runner'
        required: true
        type: string
        default: 'ubuntu-latest'
  schedule:
    - cron: '0 0 */16 * *'

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build:
    runs-on: ${{ github.event_name == 'workflow_dispatch' && inputs.runner || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      # Узнать VERSION раньше, чтобы использовать в ключах кэша
      - name: Get Chromium version
        run: |
          VERSION=$(grep -m1 -o '[0-9]\+\(\.[0-9]\+\)\{3\}' vanadium/args.gn)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "Building Chromium $VERSION"

      # ccache — ключ включает версию Chromium
      # При обновлении Chromium кэш ccache сбрасывается автоматически
      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ccache
          key: ccache-${{ env.VERSION }}-v1
          restore-keys: |
            ccache-${{ env.VERSION }}-
            ccache-

      - name: Update vanadium
        run: |
          cd vanadium
          git fetch --tags
          git checkout $(git tag | sort -V | tail -n1)
          cd ..
          git add vanadium
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          if ! git diff --staged --quiet; then
            git commit -am "update"
            git push
          fi

      - name: Set ENV
        run: |
          chmod +x *.sh
          echo "unix_time=$(date +%s)" >> $GITHUB_ENV

      - name: Build
        env:
          DEBIAN_FRONTEND: noninteractive
          LOCAL_TEST_JKS: ${{ secrets.LOCAL_TEST_JKS }}
          STORE_TEST_JKS: ${{ secrets.STORE_TEST_JKS }}
          # ccache env — передаём из workflow чтобы build.sh мог переопределить
          CCACHE_DIR: /home/runner/.cache/ccache
          CCACHE_SLOPPINESS: time_macros,include_file_mtime,include_file_ctime,file_stat_matches,file_macro,pch_defines
          CCACHE_COMPILERCHECK: content
          CCACHE_COMPRESS: "true"
        run: |
          status=0
          timeout 315m ./build.sh || status=$?
          if [ $status -eq 124 ]; then
            echo "Достигнут лимит 5ч 15м. Сохраняем кэш..."
          elif [ $status -ne 0 ]; then
            echo "Ошибка: $status"
            exit $status
          fi

      - name: ccache stats
        if: always()
        run: |
          echo "=== ccache final stats ==="
          ccache --show-stats
          echo "=== cache size ==="
          du -sh ~/.cache/ccache

      - name: Set
        if: hashFiles('chromium/src/out/release/*arm64-v8a.apk') != ''
        run: |
          export VERSION=$(find chromium/src/out/release -name '*arm64-v8a.apk' | sed 's/.*\///;s/\-arm64-v8a.apk$//')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          mv $(find chromium/src/out/release -name '*arm64-v8a.aab') $VERSION-${{ env.unix_time }}-arm64-v8a.aab
          mv $(find chromium/src/out/release -name '*arm64-v8a.apk') $VERSION-${{ env.unix_time }}-arm64-v8a.apk
          mv $(find chromium/src/out/release -name '*armeabi-v7a.apk') $VERSION-${{ env.unix_time }}-armeabi-v7a.apk

      - name: Publish
        uses: softprops/action-gh-release@v2
        if: hashFiles('chromium/src/out/release/*arm64-v8a.apk') != ''
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          name: v${{ env.VERSION }}
          tag_name: v${{ env.VERSION }}
          files: |
            ${{ env.VERSION }}-${{ env.unix_time }}-arm64-v8a.aab
            ${{ env.VERSION }}-${{ env.unix_time }}-arm64-v8a.apk
            ${{ env.VERSION }}-${{ env.unix_time }}-armeabi-v7a.apk

      - name: Attest
        uses: actions/attest-build-provenance@v4
        if: hashFiles('chromium/src/out/release/*arm64-v8a.apk') != ''
        with:
          subject-path: |
            ${{ env.VERSION }}-${{ env.unix_time }}-arm64-v8a.aab
            ${{ env.VERSION }}-${{ env.unix_time }}-arm64-v8a.apk
            ${{ env.VERSION }}-${{ env.unix_time }}-armeabi-v7a.apk
